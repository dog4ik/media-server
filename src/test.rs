#[cfg(test)]
mod tests {
    use crate::scan;
    use std::path::PathBuf;

    #[tokio::test]
    async fn test_scan() {
        let input = vec![PathBuf::from(
            "/home/dog4ik/Documents/dev/rust/media-server/test",
        )];
    }
}
